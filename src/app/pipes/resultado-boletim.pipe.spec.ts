import { ResultadoBoletimPipe } from './resultado-boletim.pipe';

describe('ResultadoBoletimPipe', () => {
  it('create an instance', () => {
    const pipe = new ResultadoBoletimPipe();
    expect(pipe).toBeTruthy();
  });
});
