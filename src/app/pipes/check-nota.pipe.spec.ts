import { CheckNotaPipe } from './check-nota.pipe';

describe('CheckNotaPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckNotaPipe();
    expect(pipe).toBeTruthy();
  });
});
